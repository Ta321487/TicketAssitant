<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="TA_WPF.Views.AdvancedQueryRoutePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TA_WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:TA_WPF.ViewModels"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:converters="clr-namespace:TA_WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">
    
    <!-- 设计时数据 -->
    <d:UserControl.DataContext>
        <viewmodels:DesignTimeAdvancedQueryRouteViewModel />
    </d:UserControl.DataContext>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
            
            <!-- 增强的MaterialDesign输入框样式 - 改善深色模式下的可见性 -->
            <Style x:Key="EnhancedMaterialDesignOutlinedTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource GlobalAccentBrush}" />
                <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="{DynamicResource GlobalAccentBrush}" />
                <Setter Property="FontSize" Value="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="materialDesign:HintAssist.Foreground" Value="{DynamicResource GlobalAccentBrush}" />
                        <Setter Property="materialDesign:HintAssist.Background" Value="{DynamicResource MaterialDesignPaper}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- 自定义ToolTip样式 -->
            <Style x:Key="CustomToolTipStyle" TargetType="ToolTip">
                <Setter Property="Background" Value="{DynamicResource PrimaryHueDarkBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{DynamicResource GlobalAccentBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="FontSize" Value="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="Black" />
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- AutoHideScrollViewer样式 -->
            <Style x:Key="AutoHideScrollViewerStyle" TargetType="ScrollViewer">
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <!-- 高级查询面板 -->
    <Border Background="{DynamicResource MaterialDesignCardBackground}"
            CornerRadius="4"
            BorderBrush="{DynamicResource GlobalAccentBrush}"
            BorderThickness="1"
            Padding="16"
            Visibility="{Binding IsQueryPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 查询条件 -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 路线名称 -->
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="路线名称" Style="{StaticResource MaterialDesignBody1TextBlock}" 
                              FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" Margin="0,0,0,4"/>
                    <Grid>
                        <TextBox Text="{Binding RouteNameFilter, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource EnhancedMaterialDesignOutlinedTextBox}"
                                materialDesign:HintAssist.Hint="输入路线名称"
                                InputMethod.IsInputMethodEnabled="True"
                                InputMethod.PreferredImeState="On"
                                InputMethod.PreferredImeConversionMode="Native"/>
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding ClearRouteNameCommand}"
                                ToolTip="清空路线名称"
                                ToolTipService.ToolTip="清空路线名称"
                                ToolTipService.InitialShowDelay="500"
                                Width="32" Height="32"
                                Padding="4"
                                Foreground="{DynamicResource GlobalAccentBrush}"
                                HorizontalAlignment="Right"
                                Margin="0,0,8,0"
                                Visibility="{Binding RouteNameFilter, Converter={StaticResource StringToVisibilityConverter}}">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                        </Button>
                        <Popup IsOpen="{Binding IsRouteNameDropdownOpen}" 
                               PlacementTarget="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}}"
                               Width="{Binding ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}}"
                               StaysOpen="False">
                            <Border Background="{DynamicResource MaterialDesignPaper}" 
                                    BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                    BorderThickness="1" 
                                    MaxHeight="200">
                                <ScrollViewer VerticalScrollBarVisibility="Auto"
                                            Style="{StaticResource AutoHideScrollViewerStyle}">
                                    <ItemsControl ItemsSource="{Binding RouteNameSuggestions}"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                KeyboardNavigation.TabNavigation="Cycle">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{Binding DataContext.SelectRouteNameCommand, 
                                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"
                                                        Content="{Binding RouteName}"
                                                        HorizontalContentAlignment="Center"
                                                        Style="{StaticResource MaterialDesignFlatButton}"
                                                        Width="{Binding ActualWidth, 
                                                                RelativeSource={RelativeSource Mode=FindAncestor, 
                                                                AncestorType=ScrollViewer}}"
                                                        FocusVisualStyle="{DynamicResource MaterialDesignFocusVisual}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </StackPanel>

                <!-- 收藏状态 -->
                <StackPanel Grid.Column="1" Margin="0,0,8,0" VerticalAlignment="Bottom">
                    <CheckBox Content="我收藏的路线" 
                              IsChecked="{Binding IsFavoriteChecked}"
                              Foreground="{DynamicResource GlobalAccentBrush}"
                              Background="{DynamicResource GlobalAccentBrush}"
                              BorderBrush="{DynamicResource GlobalAccentBrush}"
                              FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                              Margin="0,0,0,4"/>
                </StackPanel>
            </Grid>

            <!-- 总里程选择 -->
            <Grid Grid.Row="1" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="总里程区间:" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                           FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                           VerticalAlignment="Center" 
                           Margin="0,0,16,0"/>
                
                <WrapPanel Grid.Column="1" Orientation="Horizontal">
                    <RadioButton Content="100公里及以下" 
                                 IsChecked="{Binding IsDistance1Selected}" 
                                 Foreground="{DynamicResource GlobalAccentBrush}"
                                 Background="{DynamicResource GlobalAccentBrush}"
                                 BorderBrush="{DynamicResource GlobalAccentBrush}"
                                 FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                 GroupName="DistanceRange" 
                                 Margin="0,0,16,8"/>
                    
                    <RadioButton Content="100-500公里" 
                                 IsChecked="{Binding IsDistance2Selected}" 
                                 Foreground="{DynamicResource GlobalAccentBrush}"
                                 Background="{DynamicResource GlobalAccentBrush}"
                                 BorderBrush="{DynamicResource GlobalAccentBrush}"
                                 FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                 GroupName="DistanceRange"
                                 Margin="0,0,16,8"/>
                    
                    <RadioButton Content="500-1000公里" 
                                 IsChecked="{Binding IsDistance3Selected}" 
                                 Foreground="{DynamicResource GlobalAccentBrush}"
                                 Background="{DynamicResource GlobalAccentBrush}"
                                 BorderBrush="{DynamicResource GlobalAccentBrush}"
                                 FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                 GroupName="DistanceRange"
                                 Margin="0,0,16,8"/>
                    
                    <RadioButton Content="1000-2000公里" 
                                 IsChecked="{Binding IsDistance4Selected}" 
                                 Foreground="{DynamicResource GlobalAccentBrush}"
                                 Background="{DynamicResource GlobalAccentBrush}"
                                 BorderBrush="{DynamicResource GlobalAccentBrush}"
                                 FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                 GroupName="DistanceRange"
                                 Margin="0,0,16,8"/>
                    
                    <RadioButton Content="2000公里以上" 
                                 IsChecked="{Binding IsDistance5Selected}" 
                                 Foreground="{DynamicResource GlobalAccentBrush}"
                                 Background="{DynamicResource GlobalAccentBrush}"
                                 BorderBrush="{DynamicResource GlobalAccentBrush}"
                                 FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                 GroupName="DistanceRange"
                                 Margin="0,0,16,8"/>
                    
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding ClearDistanceRangeCommand}"
                            ToolTip="清除里程范围筛选"
                            ToolTipService.ToolTip="清除里程范围筛选"
                            ToolTipService.InitialShowDelay="500"
                            Width="24" Height="24"
                            Padding="2"
                            Foreground="{DynamicResource GlobalAccentBrush}"
                            VerticalAlignment="Center"
                            Visibility="{Binding SelectedDistanceRange, Converter={StaticResource NullToVisibilityConverter}}">
                        <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                    </Button>
                </WrapPanel>
            </Grid>

            <!-- 查询条件类型选择 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
                <TextBlock Text="查询条件组合方式:" 
                            Style="{StaticResource MaterialDesignBody1TextBlock}" 
                            FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                            VerticalAlignment="Center" 
                            Margin="0,0,16,0"/>
                <RadioButton Content="AND (同时满足所有条件)" 
                                IsChecked="{Binding IsAndCondition, Mode=TwoWay}" 
                                IsEnabled="{Binding HasMultipleFilters}"
                                Foreground="{DynamicResource GlobalAccentBrush}"
                                Background="{DynamicResource GlobalAccentBrush}"
                                BorderBrush="{DynamicResource GlobalAccentBrush}"
                                FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                GroupName="QueryConditionType" 
                                Margin="0,0,16,0"/>
                <RadioButton Content="OR (满足任一条件)" 
                                IsChecked="{Binding IsOrCondition, Mode=TwoWay}" 
                                IsEnabled="{Binding HasMultipleFilters}"
                                Foreground="{DynamicResource GlobalAccentBrush}"
                                Background="{DynamicResource GlobalAccentBrush}"
                                BorderBrush="{DynamicResource GlobalAccentBrush}"
                                FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                GroupName="QueryConditionType"/>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="重置" 
                        Command="{Binding ResetFilterCommand}" 
                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Foreground="{DynamicResource GlobalAccentBrush}"
                        BorderBrush="{DynamicResource GlobalAccentBrush}"
                        FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                        Margin="0,0,16,0"/>
                <Button Content="{Binding QueryButtonText}" 
                        Command="{Binding ApplyFilterCommand}" 
                        Background="{DynamicResource GlobalAccentBrush}"
                        Foreground="White"
                        FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 