<Window x:Class="TA_WPF.Views.RouteDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TA_WPF.Views"
        xmlns:viewmodels="clr-namespace:TA_WPF.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:TA_WPF.Converters"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="600" Width="950"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="{Binding MainViewModel.FontSize}"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="Microsoft YaHei"
        materialDesign:ThemeAssist.Theme="{Binding MainViewModel.IsDarkMode, Converter={StaticResource BooleanToThemeConverter}}"
        MinWidth="800" MinHeight="500">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.DataGrid.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Shadows.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TabControl.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
            <converters:BooleanToThemeConverter x:Key="BooleanToThemeConverter" />
            <SolidColorBrush x:Key="MaterialDesignDarkForeground" Color="White" />
            
            <!-- ComboBox 样式 -->
            <Style x:Key="MaterialDesignThemedComboBox" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignComboBox}">
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 内容区域 -->
        <TabControl Grid.Row="0" 
                    x:Name="MainTabControl"
                    SelectionChanged="MainTabControl_SelectionChanged"
                    Style="{StaticResource MaterialDesignTabControl}"
                    Background="{DynamicResource MaterialDesignPaper}"
                    TabStripPlacement="Top"
                    FontSize="{Binding MainViewModel.FontSize}"
                    Margin="10">
            
            <!-- 车票列表标签页 -->
            <TabItem Header="车票列表" 
                     FontSize="{Binding MainViewModel.FontSize}">
                <local:RouteTicketView DataContext="{Binding Tickets}" Margin="8" />
            </TabItem>
            
            <!-- 车站列表标签页 -->
            <TabItem Header="车站列表"
                     FontSize="{Binding MainViewModel.FontSize}">
                <local:RouteStationView DataContext="{Binding Stations}" Margin="8" />
            </TabItem>
            
            <!-- 统计摘要标签页 -->
            <TabItem Header="统计摘要"
                     FontSize="{Binding MainViewModel.FontSize}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 统计摘要操作栏 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                ToolTip="刷新统计"
                                Margin="0,0,8,0"
                                FontSize="{Binding MainViewModel.FontSize}"
                                Foreground="{DynamicResource PrimaryHueMidBrush}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="刷新统计" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <!-- 统计摘要内容 - 目前不实现具体内容 -->
                    <Border Grid.Row="1" 
                            BorderBrush="{DynamicResource MaterialDesignDivider}" 
                            BorderThickness="1"
                            Background="{DynamicResource MaterialDesignBackground}">
                        <TextBlock Text="此处将显示统计摘要" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontSize="{Binding MainViewModel.FontSize}"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- 分页控件 -->
        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：页大小选择 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="每页显示:" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>
                <ComboBox x:Name="PageSizeComboBox"
                          ItemsSource="{Binding Tickets.PaginationViewModel.PageSizeOptions}"
                          SelectedItem="{Binding Tickets.PaginationViewModel.PageSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Width="60"
                          FontSize="{Binding MainViewModel.FontSize}"
                          materialDesign:HintAssist.Hint="页大小"
                          Style="{StaticResource MaterialDesignThemedComboBox}"
                          VerticalAlignment="Center"
                          ToolTip="每页显示的记录数量"/>
                
                <!-- 总记录数和选中项数量 -->
                <TextBlock x:Name="TotalCountTextBlock"
                           Text="{Binding Tickets.TotalCount, StringFormat=总记录数: {0}}" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"/>
                
                <!-- 选择状态指示器 -->
                <TextBlock x:Name="SelectedItemsTextBlock"
                           Text="{Binding Tickets.SelectedItemsCount, StringFormat=已选择 {0} 项}"
                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           FontWeight="Medium"
                           Visibility="{Binding Tickets.HasSelectedItems, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <!-- 右侧：分页导航 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                    <Button x:Name="FirstPageButton"
                            Command="{Binding Tickets.PaginationViewModel.FirstPageCommand}" 
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="第一页"
                            Margin="0,0,4,0"
                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                            IsEnabled="{Binding Tickets.PaginationViewModel.CanNavigateToFirstPage}">
                        <materialDesign:PackIcon Kind="PageFirst" />
                    </Button>
                    <Button x:Name="PreviousPageButton"
                            Command="{Binding Tickets.PaginationViewModel.PreviousPageCommand}" 
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="上一页"
                            Margin="0,0,4,0"
                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                            IsEnabled="{Binding Tickets.PaginationViewModel.CanNavigateToPreviousPage}">
                        <materialDesign:PackIcon Kind="ChevronLeft" />
                    </Button>
                    <Border Background="{DynamicResource MaterialDesignPaper}" 
                            BorderBrush="{DynamicResource PrimaryHueMidBrush}" 
                            BorderThickness="1" 
                            CornerRadius="4"
                            Padding="8,4"
                            Margin="4,0"
                            Height="36">
                        <StackPanel Orientation="Horizontal" x:Name="PageInfoPanel" MouseLeftButtonDown="PageInfoPanel_MouseLeftButtonDown">
                            <TextBlock x:Name="CurrentPageTextBlock"
                                       Text="{Binding Tickets.PaginationViewModel.CurrentPage}" 
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" / " 
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       VerticalAlignment="Center"/>
                            <TextBlock x:Name="TotalPagesTextBlock"
                                       Text="{Binding Tickets.PaginationViewModel.TotalPages}" 
                                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBox x:Name="PageNumberInput"
                             Width="60"
                             Height="36"
                             Margin="4,0"
                             Style="{StaticResource MaterialDesignTextBox}"
                             materialDesign:HintAssist.Hint="页码"
                             VerticalAlignment="Center"
                             HorizontalContentAlignment="Center"
                             VerticalContentAlignment="Center"
                             Visibility="Collapsed"
                             KeyDown="PageNumberInput_KeyDown"
                             LostFocus="PageNumberInput_LostFocus"/>
                    <Button x:Name="NextPageButton"
                            Command="{Binding Tickets.PaginationViewModel.NextPageCommand}" 
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="下一页"
                            Margin="4,0,0,0"
                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                            IsEnabled="{Binding Tickets.PaginationViewModel.CanNavigateToNextPage}">
                        <materialDesign:PackIcon Kind="ChevronRight" />
                    </Button>
                    <Button x:Name="LastPageButton"
                            Command="{Binding Tickets.PaginationViewModel.LastPageCommand}" 
                            Style="{StaticResource MaterialDesignIconButton}"
                            ToolTip="最后一页"
                            Margin="4,0,0,0"
                            Foreground="{DynamicResource PrimaryHueMidBrush}"
                            IsEnabled="{Binding Tickets.PaginationViewModel.CanNavigateToLastPage}">
                        <materialDesign:PackIcon Kind="PageLast" />
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
        
        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" 
                             Value="0" 
                             IsIndeterminate="True" 
                             Width="48" 
                             Height="48"
                             Margin="0,0,0,8"/>
                <TextBlock Text="加载中" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}"
                           HorizontalAlignment="Center"
                           Foreground="White"
                           FontWeight="Medium"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window> 